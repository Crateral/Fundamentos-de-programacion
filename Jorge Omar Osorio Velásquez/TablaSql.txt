

/* Sintaxis de sql donde vemos crear tabla es con mayuscula*/

CREATE TABLE Persona(
id INT,
nombre VARCHAR(150),
PRIMARY KEY('id'));

/*Seleccionar el nombre de la tabla donde vamos a insertar el nombre Al inicio se coloca las palabras reservadas INSERT INTO Colocamos el nombre de la tabla.*/

SELECT * FROM Persona;
INSERT INTO (id,nombre) values (01,'Jorge Osorio');
SELECT (Columnas) from Esquema.nombreTabla;

