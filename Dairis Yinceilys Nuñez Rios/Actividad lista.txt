import java.io.*;
import java.util.*;

class Informe { 
  
    public final String[] tareas={"administrativo", "empresarial", "personal"}; 
      
    private int codigo; 
    private String tarea; 
  
    public Informe(int codigo,int indiceTarea){ 
        this.codigo= codigo; 
        this.tarea= this.tareas[indiceTarea]; 
    } 
      
      
    public int getCodigo() { 
        return codigo; 
    } 
  
    public void setCodigo(int codigo) { 
        this.codigo = codigo; 
    } 
  
    public String getTarea() { 
        return tarea; 
    } 
  
    public void setTarea(String tarea) { 
        this.tarea = tarea; 
    } 
      
    public String[] getTareas(){ 
        return tareas; 
    } 
      
    @Override 
    public String toString(){ 
        return "El informe con codigo "+codigo+" tiene la tarea de "+tarea; 
    } 
      
}

public class InformeApp {
    
    private static Stack<Informe> informes = new Stack<Informe>();
  
    public static void main(String[] args) {
          
        //Agregamos 10 informes 
        for(int i=1;i<=10;i++){ 
            // Obtenemos un numero aleatorio para el tipo de tarea
            int minimo = 0;
            int maximo = 2;
            int numeroAleatorio=(int)Math.floor(Math.random()*(minimo-(maximo+1))+(maximo+1));
            // Creamos cada informe usando el numero aleatorio
            crearInforme(i, numeroAleatorio);
        } 
          
        System.out.println("Primeros diez informes agregados: ");
        mostrarInformes();
        
        // Eliminamos todos los informes
        eliminarInformes();
        System.out.println("Todos los informes fueron eliminados"); 
        
         
        //Agregamos 5 informes mas 
        for(int i=1;i<=5;i++){
            // Obtenemos un numero aleatorio para el tipo de tarea
            int minimo = 0;
            int maximo = 2;
            int numeroAleatorio=(int)Math.floor(Math.random()*(minimo-(maximo+1))+(maximo+1));
            // Creamos cada informe usando el numero aleatorio
            crearInforme(i, numeroAleatorio);
        } 
        
        System.out.println("Se agregan 5 informes:"); 
        mostrarInformes();
          
    } 
      
    public static int generaNumeroAleatorio(int minimo, int maximo){
          
        int num=(int)Math.floor(Math.random()*(minimo-(maximo+1))+(maximo+1)); 
        return num;
    }
    
    public static void crearInforme(int codigo, int indiceTarea) {
        Informe informeNuevo = new Informe(codigo, indiceTarea);
        informes.push(informeNuevo);
    }
    
    public static void eliminarInforme() {
        informes.pop();
    }
    
    public static void mostrarInformes() {
        informes.forEach(System.out::println);
    }
    
    public static void eliminarInformes() {
        informes.clear();
    }
    
    public static void consultarInformes(int indiceTarea) {
        informes.clear();
    }
    public static void mainMostrarInforme (String[] args){
        Scanner lectura = new Scanner (System.in);
        System.out.println("mostrarInformes: ");
        String Informe = lectura.next();
        System.out.println("indiceTareaAdministrativo: ");
        int tareasAdministrativo = lectura.nextInt();
        System.out.println("mostrarInformes: " + informes + "indiceTareaAdministrativo: " + tareasAdministrativo);
        
    }
}